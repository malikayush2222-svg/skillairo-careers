import mongoose from 'mongoose';const schema=new mongoose.Schema({job:{type:mongoose.Schema.Types.ObjectId,ref:'Job',required:true},user:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true},status:{type:String,enum:['Applied','Shortlisted','Interview','Rejected','Selected'],default:'Applied'}},{timestamps:true});schema.index({job:1,user:1},{unique:true});export default mongoose.model('Application',schema);
